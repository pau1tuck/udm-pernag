query me {
    Me {
        id
        firstName
        lastName
        country
        email
        isMember
        isAdmin
    }
}
query tracks {
    Tracks {
        id
        artist
        title
        version
        label
        youTubeId
        points
        updatedAt
    }
}
